 # ◦ Define variables: model_name = "GPT-4", tokens_used = 123, and cost_per_token = 0.001.

#2. Calculation: Calculate the total_cost (tokens_used * cost_per_token).

tokens_used = 123
cost_per_token =0.001

total_cost = (tokens_used * cost_per_token)

print(total_cost)
print(f"{total_cost:.4f}")
print(f'total cost = {total_cost:.4f}')